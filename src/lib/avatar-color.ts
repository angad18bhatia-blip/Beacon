const PALETTE = ["--accent", "--accent2", "--pink", "--teal", "--amber"];

export function avatarColorVar(seed: string): string {
  let hash = 0;
  for (let i = 0; i < seed.length; i++) {
    hash = (hash * 31 + seed.charCodeAt(i)) >>> 0;
  }
  return PALETTE[hash % PALETTE.length];
}
